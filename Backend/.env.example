# =============================================================================
# CONFIGURATION NAYA TRAVEL JOURNAL 
# =============================================================================
# 
# INSTRUCTIONS POUR YASSINE :
# 1. Copie ce fichier : cp .env.example .env
# 2. Modifie les valeurs dans ton nouveau fichier .env
# 3. NE PUSH JAMAIS ton fichier .env (il contient tes clés secrètes)
# 
# =============================================================================

# Configuration Flask (Backend)
FLASK_ENV=development
FLASK_DEBUG=true
FLASK_HOST=127.0.0.1
FLASK_PORT=5000

# Clés de sécurité (CHANGE CES VALEURS dans ton .env)
SECRET_KEY=naya-secret-key-a-changer
JWT_SECRET_KEY=naya-jwt-key-a-changer

# Base de données
DATABASE_URL=sqlite:///naya_travel_journal.db
DEV_DATABASE_URL=sqlite:///naya_dev.db

# API Google Maps (pour afficher les lieux)
# Va sur https://console.cloud.google.com pour obtenir ta clé
GOOGLE_MAPS_API_KEY=ta-cle-google-maps-ici

# Dossier pour les photos uploadées
UPLOAD_FOLDER=uploads

# CORS pour le frontend (adapte selon ton port frontend)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Redis (optionnel - pour limitation de requêtes)
REDIS_URL=memory://